# Sample Hello Plugin

This plugin is a minimal example used in InferencerC Feature 15 docs.

Files:
- `manifest.json` - plugin metadata and contributions
- `index.js` - sample runtime registration

Install:
1. Open Plugin Manager.
2. Choose install from manifest.
3. Select this folder's `manifest.json`.
4. Enable plugin and run `hello-command` from command palette.
